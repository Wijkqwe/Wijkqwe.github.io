[TOC]

广义系统结构定义
指令集结构、组成、硬件

# 定量分析技术
四个定量原理

* 加快经常性事件


* Amdahl定律

	S_n = 1 / ((1 - F_e) + F_e / S_e)

	加速比定义：S_n = 新速度/旧速度 = 旧时间/新时间 = T_0 / T_n

	改进后时间定义：T_n = T_0(1 - F_e + F_e / S_e)

	


* CPU性能公式
	CPU时间 = IC(指令数) * CPI(平均指令周期数) * 时钟周期时间(时钟频率的倒数)
	
	总时钟周期数 = CPI/IC

	CPI = 指令执行时间 + 每条指令平均访存次数 * 不命中率 * 不命中开销


* 局部性原理



# 计算机系统性能评测

## 性能

### 响应时间

### 吞吐率

# 提高并行性的技术途径

* 时间重叠
* 资源重复
* 资源共享



# 流水线

## 概念

### 流水线技术

* 把一个重复的过程分解为若干个子过程，每个子过程由专门的功能部件来实现
* 把多个处理过程在时间上错开，依次通过各功能段，这样，每个子过程就可以与其他的子过程并行进行



## 形成条件

多个密集且相同或类似的任务的场景


## 技术特点


## 性能指标


## 吞吐率

### 定义
在单位时间内流水线所完成的任务数量或输出结果的数量

TP = n(任务数) / T_k(处理n个任务所需时间)

### 各段时间相等

#### 实际吞吐率

TP = n / ((k + n - 1)\deltaΔ t)

#### 最大吞吐率

TP_max = lim_{n \to \infty} n / ((k + n - 1)\delta t) = 1 / (\delta t)

TP = (n / (k + n - 1))TP_max

当n趋近于k时，TP趋近于TP_max


### 各段时间不全相等

T_k＝∑Δt_i＋(n - 1)max(Δt_i)

#### 实际吞吐率

TP = n / (∑Δt_i＋(n - 1)max(Δt_i))

#### 最大吞吐率

TP_max = 1 / max(Δt_i)



## 流水线瓶颈问题

### 解决流水线瓶颈的方法
* 细分瓶颈段
* 重复设置瓶颈段


## 加速比
完成同样任务，使用流水线与不使用流水线时间比
S = T(不使用) / T(使用)

### 各段时间均等

#### 实际加速比
S = nk / (k + n - 1)

#### 最大加速比
S = k

### 各段时间不等



## 效率
流水线中的设备实际使用时间与整个运行时间的比值

### 各段时间均等

各段效率相等 e = nΔt / T_k = n / (k + n - 1)

整条流水线效率 E = ke / k = n / (k + n - 1)

E = (设备实际使用时间) / (段数 * 流水线通过的时间)

最高效率 E_max = 1

效率与吞吐率成正比 E=TP·△t

流水线的效率是流水线的实际加速比S与它
的最大加速比k的比值


### 各段时间不全相等





## 相关
* 两条指令之间存在某种依赖关系。
* 如果两条指令相关，则它们就有可能不能在流水线中重叠执行或者只能部分重叠执行

### 类
*  数据相关
*  名相关
*  控制相关

#### 数据相关
数据相关具有传递性


#### 名相关
* 反相关
* 输出相关

##### 反相关
如果指令j写的名与指令i读的名相同，则称指令i和j发生了反相关。
• 指令j写的名＝指令i读的名


##### 输出相关
如果指令j和指令i写相同的名，则称指令i和j发生了输出相关
• 指令j写的名＝指令i写的名


##### 解决方案
* 换名技术



#### 控制相关



### 5段RISC流水线

#### 取指令周期（IF）


#### 指令译码/读寄存器周期（ID）


#### 执行/有效地址计算周期（EX）


#### 存储器访问／分支完成周期（MEM）


#### 写回周期（WB）



## 冲突

### 问题
* 导致错误的执行结果。
* 流水线可能会出现停顿，从而降低流水线的效率和实际的加速比。


### 基本解决
* 暂停部分指令执行


### 分类
* 结构冲突
* 数据冲突
* 控制冲突


### 结构冲突

#### 解决方案
* 插入暂停周期
* 设置相互独立的存储器，使之分别存储指令与数据



### 数据冲突
* 写后读冲突（RAW）
* 写后写冲突（WAW）
* 读后写冲突（WAR）



# 存储系统

## 理想存储器
* 容量大
* 存取时间短
* 价格低
* 无码率低
* 保存时间长
* 功耗低

## 命中率H不命中率F
设由M1，M2组成的两级存储层次，N_1访问M1的次数，N_2访问M2的次数

H = N_1 / (N_1 + N_2)

F = 1 - H

## CPU一次访存两种情况

### 命中时
访问时间T_1


### 不命中时
访问时间 = T_1 + T_2 + T_B = T_1 + T_M

T_M = T_2 + T_B

传送一个信息块所需时间T_B


## 平均访问时间T_A

T_A = T_1 + F * T_M

T_1为M1的访问时间


## cache
* 放
* 找
* 写
* 换

### 放

#### 全相联映像
主存任意一块可映象到cache中任意一块


#### 组相联映像
主存和cache相同大小划为块和组

主存和cache的组之间直接映像，两个对应组内部全相联映像


#### 直接映像
主存中一块只映象到cache中特定一块



### 换
不命中，且已满或未满但块冲突

#### 替换算法
* 直接映像

* 全相联、组相联
	* 随机法
	* 先入先出FIFO
	* 最近最少使用LFU



### 改进性能
* 降低不命中率
* 减小不命中开销
* 减少cacha命中时间

### 优化技术

#### 降低不命中率
* 强制不命中：增加块大小
* 容量不命中：增加容量
* 冲突不命中：提高相联性(理想全相联)

* 增加cache块大小
* 增加cache容量
* 提高相联度
* 伪相联
* 硬件预取
* 编译器控制的预取
* 编译器优化
* “牺牲”cache


#### 减小不命中开销
* 采用两级cache
* 让读不命中优先于写



