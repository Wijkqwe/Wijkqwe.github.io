# CMake

## 生成器表达式

语法 `$<...>`, 它的值会在构建系统生成时或实际编译时才被计算.

### 分类

根据文档中的标准区分.

```cmake
$<qwe:asd>
```

- 条件表达式: qwe 是能得出布尔值的表达式, asd 是条件为真时的返回值.
- 信息表达式: qwe 是一个查询命令, asd 是参数.

### 内置表达式

- COMPILE_LANGUAGE: CMake 3.0 版本之后被引入。通过 `$<COMPILE_LANGUAGE:lang1,lang2,...>` 作为**信息表达式**使用，返回一个布尔值。
