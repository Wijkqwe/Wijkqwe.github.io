# Python

## pip

## 虚拟环境

### venv

Python 3.3+ 版本提供了一个叫做 venv 的模块.

创建虚拟环境.

```bash
python -m venv <envname>
```

激活虚拟环境.

```bash
$ source <envname>/bin/activate
```

```bash
(<envname>) $
```

退出虚拟环境.
```bash
(<envname>) $ deactivate
```

